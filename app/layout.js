import "./globals.css";

// Root layout is intentionally minimal — the real <html>/<body> with the
// correct lang and direction is set in app/[lang]/layout.js.
export default function RootLayout({ children }) {
  return children;
}
